function sum(numbers) {
  var res = 0
  numbers.forEach(function(number) {
    res += parseFloat(number)
  })
  return res
}

function mult(numbers) {
  var res = 0
  numbers.forEach(function(number) {
    res += parseFloat(number)
  })
  return res
}